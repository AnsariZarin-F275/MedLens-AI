"use client";

import Link from "next/link";
import { ArrowLeft, ArrowRight, BrainCircuit, Check, ChevronLeft, ClipboardList, HeartPulse, ImagePlus, ShieldCheck, UserRound } from "lucide-react";
import { AnimatePresence, motion } from "framer-motion";
import { useState } from "react";

const steps = [
  { label: "Personal", icon: UserRound },
  { label: "History", icon: ClipboardList },
  { label: "Symptoms", icon: HeartPulse },
  { label: "Review", icon: ShieldCheck },
];

const inputClass = "mt-2 w-full rounded-xl border border-slate-200 bg-white px-3.5 py-3 text-sm text-slate-900 outline-none transition placeholder:text-slate-400 focus:border-blue-500 focus:ring-4 focus:ring-blue-100";

function Field({ label, hint, children }) {
  return <label className="block text-sm font-semibold text-slate-700"><span>{label}</span>{hint && <span className="ml-1 font-normal text-slate-400">{hint}</span>}{children}</label>;
}

function TextInput({ label, hint, ...props }) {
  return <Field label={label} hint={hint}><input className={inputClass} {...props} /></Field>;
}

function Choice({ label, name, value, selected, onChange }) {
  return <button type="button" onClick={() => onChange(value)} className={`rounded-xl border px-4 py-3 text-sm font-semibold transition ${selected === value ? "border-blue-600 bg-blue-600 text-white shadow-lg shadow-blue-600/20" : "border-slate-200 bg-white text-slate-600 hover:border-blue-200 hover:bg-blue-50"}`} aria-pressed={selected === value}>{label}</button>;
}

export default function AnalyzeForm() {
  const [step, setStep] = useState(0);
  const [form, setForm] = useState({ gender: "", breathing: "", chestPain: "", pain: 5 });
  const setValue = (name, value) => setForm((current) => ({ ...current, [name]: value }));
  const progress = ((step + 1) / steps.length) * 100;

  const content = [
    <div key="personal" className="grid gap-5 sm:grid-cols-2"><TextInput label="Name" hint="(optional)" type="text" placeholder="Your name" /><TextInput label="Age" type="number" min="0" placeholder="Years" /><Field label="Gender"><select className={inputClass} value={form.gender} onChange={(event) => setValue("gender", event.target.value)}><option value="">Select gender</option><option>Female</option><option>Male</option><option>Non-binary</option><option>Prefer not to say</option></select></Field><div className="grid grid-cols-2 gap-3"><TextInput label="Height" type="number" min="0" placeholder="cm" /><TextInput label="Weight" type="number" min="0" placeholder="kg" /></div></div>,
    <div key="history" className="space-y-5"><Field label="Existing conditions"><textarea className={inputClass} rows="3" placeholder="For example: diabetes, asthma, high blood pressure" /></Field><Field label="Current medications"><textarea className={inputClass} rows="3" placeholder="List medicines, supplements, or treatments you take" /></Field><Field label="Allergies"><textarea className={inputClass} rows="3" placeholder="List known allergies, including medicines or foods" /></Field></div>,
    <div key="symptoms" className="space-y-5"><Field label="Main symptoms"><textarea className={inputClass} rows="4" placeholder="Describe what you are experiencing, where it hurts, and anything that makes it better or worse" /></Field><div className="grid gap-5 sm:grid-cols-2"><TextInput label="Duration" type="text" placeholder="For example: 2 days" /><TextInput label="Body temperature" type="number" step="0.1" placeholder="°C" /></div><Field label="Pain level"><div className="mt-3 flex items-center gap-4"><input className="h-2 w-full cursor-pointer appearance-none rounded-full bg-blue-100 accent-blue-600" type="range" min="1" max="10" value={form.pain} onChange={(event) => setValue("pain", event.target.value)} aria-valuetext={`${form.pain} out of 10`} /><output className="grid size-10 shrink-0 place-items-center rounded-xl bg-blue-600 text-sm font-bold text-white">{form.pain}</output></div><p className="mt-2 flex justify-between text-xs text-slate-400"><span>Mild</span><span>Severe</span></p></Field><div className="grid gap-5 sm:grid-cols-2"><fieldset><legend className="text-sm font-semibold text-slate-700">Breathing difficulty</legend><div className="mt-2 flex gap-2"><Choice label="No" value="no" selected={form.breathing} onChange={(value) => setValue("breathing", value)} /><Choice label="Yes" value="yes" selected={form.breathing} onChange={(value) => setValue("breathing", value)} /></div></fieldset><fieldset><legend className="text-sm font-semibold text-slate-700">Chest pain</legend><div className="mt-2 flex gap-2"><Choice label="No" value="no" selected={form.chestPain} onChange={(value) => setValue("chestPain", value)} /><Choice label="Yes" value="yes" selected={form.chestPain} onChange={(value) => setValue("chestPain", value)} /></div></fieldset></div></div>,
    <div key="review" className="space-y-5"><div className="rounded-2xl border border-blue-100 bg-blue-50/70 p-5"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-blue-600 text-white"><ImagePlus size={20} /></span><div><h3 className="font-bold text-slate-900">Add an image <span className="font-normal text-slate-400">(optional)</span></h3><p className="mt-1 text-sm leading-6 text-slate-600">Upload a clear photo that may help describe a visible symptom.</p></div></div><label className="mt-5 flex cursor-pointer items-center justify-center rounded-xl border border-dashed border-blue-300 bg-white px-4 py-8 text-center text-sm font-semibold text-blue-600 transition hover:border-blue-500 hover:bg-blue-50"><input type="file" accept="image/*" className="sr-only" />Choose image</label></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><strong>Important:</strong> MedLens provides informational support only. If you have severe chest pain, serious breathing difficulty, or a life-threatening emergency, contact local emergency services immediately.</div></div>,
  ];

  return <main className="min-h-screen overflow-hidden bg-gradient-to-br from-blue-50 via-white to-cyan-50 px-4 py-5 sm:px-6 sm:py-8"><div className="mx-auto max-w-4xl"><header className="flex items-center justify-between"><Link href="/" className="inline-flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold text-slate-600 transition hover:bg-white hover:text-blue-600"><ArrowLeft size={17} /> Back to home</Link><div className="flex items-center gap-2 text-slate-950"><span className="grid size-9 place-items-center rounded-xl bg-blue-600 text-white"><BrainCircuit size={19} /></span><span className="font-bold tracking-tight">MedLens AI</span></div></header><motion.section initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.45 }} className="mt-8 rounded-3xl border border-white/80 bg-white/80 p-5 shadow-2xl shadow-blue-950/10 backdrop-blur sm:mt-12 sm:p-8"><div className="max-w-2xl"><p className="text-sm font-bold uppercase tracking-[0.16em] text-blue-600">Symptom assessment</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">Help us understand what&apos;s going on.</h1><p className="mt-3 text-sm leading-6 text-slate-600 sm:text-base">Share a few details so MedLens can organize your concerns into clear, practical next steps.</p></div><ol className="mt-8 grid grid-cols-4 gap-2" aria-label="Form progress">{steps.map(({ label, icon: Icon }, index) => <li key={label} className="min-w-0"><div className="flex items-center gap-2"><span className={`grid size-8 shrink-0 place-items-center rounded-full text-xs font-bold ${index <= step ? "bg-blue-600 text-white" : "bg-slate-100 text-slate-400"}`}>{index < step ? <Check size={15} /> : <Icon size={15} />}</span><span className={`hidden truncate text-sm font-semibold sm:block ${index <= step ? "text-slate-800" : "text-slate-400"}`}>{label}</span></div></li>)}</ol><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-slate-100"><motion.div className="h-full rounded-full bg-blue-600" animate={{ width: `${progress}%` }} /></div><form onSubmit={(event) => event.preventDefault()} className="mt-8"><AnimatePresence mode="wait"><motion.div key={step} initial={{ opacity: 0, x: 12 }} animate={{ opacity: 1, x: 0 }} exit={{ opacity: 0, x: -12 }} transition={{ duration: 0.2 }}>{content[step]}</motion.div></AnimatePresence><div className="mt-8 flex flex-col-reverse gap-3 border-t border-slate-100 pt-6 sm:flex-row sm:justify-between"><button type="button" onClick={() => setStep((current) => Math.max(0, current - 1))} disabled={step === 0} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-600 transition hover:border-blue-200 hover:bg-blue-50 disabled:cursor-not-allowed disabled:opacity-40"><ChevronLeft size={17} /> Previous</button>{step < steps.length - 1 ? <button type="button" onClick={() => setStep((current) => Math.min(steps.length - 1, current + 1))} className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 transition hover:-translate-y-0.5 hover:bg-blue-700">Continue <ArrowRight size={17} /></button> : <Link href="/results" className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 transition hover:-translate-y-0.5 hover:bg-blue-700">Analyze with AI <BrainCircuit size={17} /></Link>}</div></form></motion.section><p className="mx-auto mt-5 max-w-2xl text-center text-xs leading-5 text-slate-500">Your details are used only to structure this assessment. This page does not replace professional medical care.</p></div></main>;
}
