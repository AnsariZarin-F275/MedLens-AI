"use client";

import Link from "next/link";
import { Activity, AlertTriangle, ArrowLeft, ArrowRight, CheckCircle2, ChevronRight, ClipboardList, Download, HeartPulse, Home, RefreshCw, ShieldAlert, Sparkles, Stethoscope, Thermometer, UserRound } from "lucide-react";
import { motion } from "framer-motion";
import { useEffect, useState } from "react";
import { readAnalysisResult } from "@/lib/analysis-result";
import { downloadAnalysisReport } from "@/lib/pdf-report";

const riskStyles = {
  low: "border-emerald-200 bg-emerald-50 text-emerald-800",
  moderate: "border-yellow-200 bg-yellow-50 text-yellow-800",
  high: "border-orange-200 bg-orange-50 text-orange-800",
  critical: "border-red-200 bg-red-50 text-red-800",
};

function getRiskStyle(riskLevel) {
  return riskStyles[riskLevel.toLowerCase()] || riskStyles.low;
}

function GlassCard({ title, icon: Icon, children, className = "", delay = 0 }) {
  return <motion.article initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.35, delay }} className={`rounded-3xl border border-white/80 bg-white/75 p-5 shadow-xl shadow-blue-950/5 backdrop-blur-xl sm:p-6 ${className}`}><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon size={20} /></span><h2 className="text-lg font-bold tracking-tight text-slate-900">{title}</h2></div><div className="mt-5">{children}</div></motion.article>;
}

function List({ items, variant = "check" }) {
  const Icon = variant === "timeline" ? ChevronRight : CheckCircle2;
  return <ul className="space-y-3">{items.map((item, index) => <li key={`${item}-${index}`} className="flex gap-3 text-sm leading-6 text-slate-600"><span className={`grid size-5 shrink-0 place-items-center rounded-full ${variant === "timeline" ? "bg-blue-600 text-white" : "text-blue-600"}`}>{variant === "timeline" ? <span className="text-[10px] font-bold">{index + 1}</span> : <Icon size={17} />}</span><span>{item}</span></li>)}</ul>;
}

function EmptyState() {
  return <main className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-cyan-50 px-4 py-5 sm:px-6 sm:py-8"><div className="mx-auto flex min-h-[70vh] max-w-xl flex-col items-center justify-center text-center"><span className="grid size-12 place-items-center rounded-2xl bg-blue-100 text-blue-600"><ClipboardList size={24} /></span><h1 className="mt-5 text-2xl font-bold text-slate-950">No analysis report found</h1><p className="mt-3 text-sm leading-6 text-slate-600">Complete a symptom assessment to view your medical triage summary.</p><p className="mt-2 text-xs text-slate-500">PDF reports become available once an analysis has been completed.</p><Link href="/analyze" className="mt-6 inline-flex items-center gap-2 rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-600/20">Analyze symptoms <ArrowRight size={17} /></Link></div></main>;
}

export default function ResultsDashboard() {
  const [report, setReport] = useState(undefined);

  useEffect(() => {
    const frame = requestAnimationFrame(() => setReport(readAnalysisResult()));
    return () => cancelAnimationFrame(frame);
  }, []);

  if (report === undefined) return <main className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-cyan-50" />;
  if (!report) return <EmptyState />;

  const emergencyCopy = report.emergency ? "Emergency care is recommended. Contact local emergency services or seek immediate in-person care." : "Emergency care is not currently indicated. Continue monitoring symptoms and follow the recommended next steps.";

  return <main className="min-h-screen overflow-hidden bg-gradient-to-br from-blue-50 via-white to-cyan-50 px-4 py-5 sm:px-6 sm:py-8"><div className="mx-auto max-w-7xl"><header className="flex items-center justify-between"><Link href="/" className="inline-flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold text-slate-600 transition hover:bg-white hover:text-blue-600"><ArrowLeft size={17} /> Back to home</Link><Link href="/" className="flex items-center gap-2 text-slate-950"><span className="grid size-9 place-items-center rounded-xl bg-blue-600 text-white"><HeartPulse size={19} /></span><span className="font-bold tracking-tight">MedLens AI</span></Link></header><nav className="mt-8 flex items-center gap-1 text-sm text-slate-500" aria-label="Breadcrumb"><Home size={15} /><Link href="/" className="rounded px-1 transition hover:text-blue-600">Home</Link><ChevronRight size={15} /><Link href="/analyze" className="rounded px-1 transition hover:text-blue-600">Analyze</Link><ChevronRight size={15} /><span className="px-1 font-medium text-slate-800">Results</span></nav><motion.header initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.4 }} className="mt-7 flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-sm font-bold uppercase tracking-[0.16em] text-blue-600">MedLens assessment</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950 sm:text-5xl">Medical Analysis Report</h1><p className="mt-3 text-base text-slate-600">AI-generated emergency triage summary</p></div><div className="inline-flex items-center gap-2 self-start rounded-full border border-blue-100 bg-white/80 px-3 py-2 text-xs font-semibold text-slate-600 shadow-sm"><Activity size={15} className="text-blue-600" /> Assessment complete</div></motion.header><motion.aside initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.1 }} className={`mt-7 flex gap-4 rounded-2xl border p-5 ${report.emergency ? "border-red-200 bg-red-50 text-red-900" : "border-emerald-200 bg-emerald-50 text-emerald-900"}`}><span className={`grid size-10 shrink-0 place-items-center rounded-xl ${report.emergency ? "bg-red-600 text-white" : "bg-emerald-600 text-white"}`}><ShieldAlert size={20} /></span><div><h2 className="font-bold">{report.emergency ? "Emergency action recommended" : "No immediate emergency detected"}</h2><p className="mt-1 text-sm leading-6">{emergencyCopy}</p></div></motion.aside><section className="mt-5 grid gap-5 lg:grid-cols-3"><GlassCard title="Risk level" icon={ShieldAlert} delay={0.05}><div className={`inline-flex items-center gap-2 rounded-2xl border px-4 py-3 text-lg font-bold ${getRiskStyle(report.riskLevel)}`}><AlertTriangle size={19} />{report.riskLevel}</div><p className="mt-4 text-sm leading-6 text-slate-600">{report.urgency}</p></GlassCard><GlassCard title="Professional summary" icon={ClipboardList} className="lg:col-span-2" delay={0.1}><p className="text-sm leading-7 text-slate-600">{report.summary}</p></GlassCard><GlassCard title="Possible conditions" icon={Thermometer} className="lg:col-span-2" delay={0.15}><div className="flex flex-wrap gap-2">{report.possibleConditions.map((condition) => <span key={condition} className="rounded-xl border border-blue-100 bg-gradient-to-br from-blue-50 to-white px-3.5 py-2 text-sm font-semibold text-blue-800 shadow-sm">{condition}</span>)}</div><p className="mt-4 text-xs leading-5 text-slate-500">These are possible explanations only and not definitive diagnoses.</p></GlassCard><GlassCard title="Specialist recommendation" icon={Stethoscope} delay={0.2}><p className="text-lg font-bold text-slate-900">{report.recommendedSpecialist}</p><p className="mt-2 text-sm leading-6 text-slate-600">A consultation can help determine appropriate evaluation and treatment.</p></GlassCard><GlassCard title="First aid guidance" icon={HeartPulse} delay={0.25}><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-1">{report.firstAid.map((item, index) => <div key={`${item}-${index}`} className="flex gap-3 rounded-2xl border border-blue-100 bg-blue-50/60 p-3.5"><span className="grid size-7 shrink-0 place-items-center rounded-lg bg-white text-blue-600 shadow-sm"><CheckCircle2 size={16} /></span><p className="text-sm leading-6 text-slate-700">{item}</p></div>)}</div></GlassCard><GlassCard title="Next steps" icon={ArrowRight} className="lg:col-span-2" delay={0.3}><List items={report.nextSteps} variant="timeline" /></GlassCard><GlassCard title="Emergency status" icon={Sparkles} delay={0.35}><div className={`rounded-2xl p-4 ${report.emergency ? "bg-red-50 text-red-800" : "bg-emerald-50 text-emerald-800"}`}><p className="font-bold">{report.emergency ? "Urgent action needed" : "Continue care plan"}</p><p className="mt-2 text-sm leading-6">{report.emergency ? "Seek immediate medical help." : "Monitor your symptoms and follow the guidance above."}</p></div></GlassCard></section><motion.aside initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.35 }} className="mt-5 rounded-2xl border border-amber-200 bg-amber-50/80 p-4 text-sm leading-6 text-amber-900"><strong>Medical disclaimer:</strong> This is an informational triage summary, not a medical diagnosis. Contact emergency services immediately for a life-threatening emergency.</motion.aside><section className="mt-8 flex flex-col gap-4 border-t border-slate-200 py-8 lg:flex-row lg:items-center lg:justify-between"><p className="text-sm text-slate-500">Keep this report handy for your consultation.</p><div className="flex flex-col gap-3 sm:flex-row"><button type="button" onClick={() => downloadAnalysisReport(report)} className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700 transition hover:border-blue-200 hover:bg-blue-50"><Download size={17} /> Download PDF Report</button><Link href="/analyze" className="inline-flex items-center justify-center gap-2 rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm font-semibold text-blue-700 transition hover:bg-blue-100"><RefreshCw size={17} /> Analyze Again</Link><Link href="/" className="inline-flex items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 py-3 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 transition hover:-translate-y-0.5 hover:bg-blue-700">Back to Home <ArrowRight size={17} /></Link></div></section></div></main>;
}
